@model IEnumerable<Online_Exam_Project_Main.Models.Exam_tbl>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.exam_title)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Subject_tbl.subject_title)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.exam_title)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Subject_tbl.subject_title)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.exam_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.exam_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.exam_id })
        </td>
    </tr>
}

</table>
